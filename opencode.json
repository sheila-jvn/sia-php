{
  "$schema": "https://opencode.ai/config.json",

  "mcp": {
    "playwright": {
      "type": "local",
      "command": [
        "docker",
        "container",
        "run",
        "-i",
        "--rm",
        "--init",
        "mcr.microsoft.com/playwright/mcp"
      ],
      "enabled": true
    }
  }
}
